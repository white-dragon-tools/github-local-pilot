{
  "name": "main-repo",
  "version": "1.9.0",
  "private": true,
  "description": "GitHub to local workspace navigation tool",
  "repository": {
    "type": "git",
    "url": "https://github.com/white-dragon-tools/github-local-pilot.git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
